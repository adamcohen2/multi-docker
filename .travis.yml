sudo: required
# services:
#   - docker

# before_install:
#   - docker build -t adamcohen/react-test -f ./client/Dockerfile.dev ./client

# script:
#   - docker run -e CI=true adamcohen/react-test npm test

# after_success:
#   - docker build -t adamcohen/multi-client ./client
#   - docker build -t adamcohen/multi-nginx ./nginx
#   - docker build -t adamcohen/multi-server ./server
#   - docker build -t adamcohen/multi-worker ./worker
